from distutils.core import setup

setup(
    name = 'Desktop entries Contrib Libraries',
    version = '0.1',
    package_dir = {'cream.contrib.desktopentries': 'desktopentries'},
    packages = ['cream.contrib.desktopentries'],
    )
